# TSP Sub Account — Livestream Performance Dashboard

Dashboard 1 file HTML (Chart.js CDN) cho báo cáo livestream TSP Sub Account CPD.

- Nguồn actual: `[KT] Raw Compare` (T1–T5) + `Raw Centralized` (T6+) — file `[Internal] Report_TSP Sub Account_CPD.xlsx`
- Plan: Google Sheet `[Internal] TSP Sub Acc | Ads Monthly Plan` (NMV = H46, Ads = E46; tháng cũ F1/F3)
- Build: `pipeline/build_dashboard.ps1` (xem `D:\Data Sub\pipeline\`)
- Có password gate phía client (`password.js`).

> Deploy: GitHub Pages, branch `main`, root.
